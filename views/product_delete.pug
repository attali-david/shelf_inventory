extends layout

block content
  if item
    h1.li4 #{title} : #{item.name} 
  p Are you sure you want to delete this product?
  div.form-group
    form(method='POST' action='')
      input#formula.form-content(type='hidden' name='itemid' value=item._id)
      button.btn.btn-primary(type='submit') Delete